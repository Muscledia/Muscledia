server:
  port: 8080

spring:
  application:
    name: muscledia-api-gateway
  cloud:
    gateway:
      routes:
        # User Service Routes (No Auth for login/register)
        - id: auth-public
          uri: http://localhost:8081
          predicates:
            - Path=/api/users/login,/api/users/register
          filters:
            - StripPrefix=0

        # User Service Routes (Protected)
#        - id: user-protected
#          uri: http://localhost:8081
#          predicates:
#            - Path=/api/users/**
#          filters:
#            - StripPrefix=0
#            - name: AuthenticationFilter
#              args:
#                enabled: true

        # Workout Service Routes (Public)
        - id: workout-public
          uri: http://localhost:8082
          predicates:
            - Path=/api/v1/**
          filters:
            - StripPrefix=0

#        # Workout Service Routes (Protected)
#        - id: workout-protected
#          uri: http://localhost:8082
#          predicates:
#            - Path=/api/v1/**
#          filters:
#            - StripPrefix=0
#            - name: AuthenticationFilter
#              args:
#                enabled: true

        # Gamification Service Routes
        - id: gamification-route
          uri: http://localhost:8083
          predicates:
            - Path=/api/gamification/**
          filters:
            - StripPrefix=0
#            - name: AuthenticationFilter  # Custom filter for JWT validation
#              args:
#                enabled: true

      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Credentials Access-Control-Allow-Origin
        - name: Retry
          args:
            retries: 3
            methods: GET,POST
            backoff:
              firstBackoff: 50ms
              maxBackoff: 500ms




eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: "gateway,health,info"
  endpoint:
    gateway:
      enabled: true


# JWT Configuration
jwt:
  secret: ${JWT_SECRET:81795ad725b2cadd49d27a60438415588db374020a561ae19cafebeef6339655304975b150867b21d3715e341a49271a75a7dde39776e156af0ddad50f5e6ec3}
  expiration: ${JWT_EXPIRATION:86400000} # 24 hours in milliseconds
  issuer: ${JWT_ISSUER:muscledia-user-service}
  header: Authorization
  prefix: "Bearer "

logging:
  level:
    com.muscledia.api_gateway: DEBUG
    org.springframework.cloud.gateway: DEBUG
    reactor.netty: DEBUG